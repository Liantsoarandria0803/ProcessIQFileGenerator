# 🚨 ERREUR 404 - SOLUTION

## Le problème
```
POST /api/admission/candidates → 404 "Route non trouvée"
```

## La cause
Le serveur Node.js utilise une **ancienne version du code** compilée avant l'ajout des nouvelles routes.

## ✅ LA SOLUTION (3 commandes)

### Option 1 : Script automatique (RECOMMANDÉ)
```bash
cd /home/liantsoa/Desktop/Work/process-IQ-rush-school-main/backend/node-api
./restart-server.sh
```

### Option 2 : Commandes manuelles
```bash
# 1. Arrêter le serveur actuel
lsof -ti:8001 | xargs kill -9

# 2. Aller dans le dossier
cd /home/liantsoa/Desktop/Work/process-IQ-rush-school-main/backend/node-api

# 3. Redémarrer en mode dev
npm run dev
```

## ✅ Vérification

Après le redémarrage, vous devriez voir :
```
🚀 Serveur démarré sur le port 8001
📍 URL: http://localhost:8001
```

Puis testez dans Swagger : **http://localhost:8001/api-docs**

## 📊 Routes disponibles après redémarrage

| Méthode | Route | Description |
|---------|-------|-------------|
| **POST** | `/api/admission/candidates` | **✅ Créer un candidat** |
| **GET** | `/api/admission/candidates/:id` | **✅ Récupérer un candidat** |
| **PUT** | `/api/admission/candidates/:id` | **✅ Mettre à jour** |
| **DELETE** | `/api/admission/candidates/:id` | **✅ Supprimer** |

## 🎯 Test rapide

```bash
curl -X POST http://localhost:8001/api/admission/candidates \
  -H "Content-Type: application/json" \
  -d '{"prenom":"Jean","nom_naissance":"Dupont",...}'
```

---

**IMPORTANT** : Le serveur doit être redémarré pour que les nouvelles routes soient prises en compte !
